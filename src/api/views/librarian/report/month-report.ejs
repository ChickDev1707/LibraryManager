<%- contentFor('head') %> 
<title>Month Report</title>
<!-- Global src -->
<script type="text/javascript" src="https://unpkg.com/xlsx@0.15.1/dist/xlsx.full.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.6.0/chart.min.js" ></script>
<link rel="stylesheet" href="../../../../public/css/pages/librarian/report.css">
<!-- Local src -->
<script src="../../../../public/js/chart-report.js"></script>
<script src="../../../../public/js/export-report.js"></script>

<%- contentFor('body') %> 
<%- include('../../partials/header/librarian-header.ejs') %> 

<body onload="onLoad('<%= data %>', '<%= label %>', '<%= ratio %>')">
    <div class="page-content-wrapper">
        <form id="form-picker" class="row" action="/librarian/month-report" method="get" >
            <div class="col-7"></div>
            <input class="col form-control" type="month" name="month" value="<%= month %>">
            <button class="col-1 submit-btn" type="submit" >Submit</button>
            <button id="btnExport" class="col-1 submit-btn" type="button" onclick="fnExportToExcel('xlsx')"><i class="fas fa-file-export"></i> Export</button>
        </form>
        <h2 class="text-center">THỐNG KÊ TÌNH HÌNH MƯỢN SÁCH THEO THỂ LOẠI</h2>
        <div id="div-chart" class="row">
            <div class="col-8">
                <canvas id="myBarChart"></canvas>
            </div>
            <div class="col-4">
                <canvas id="myPieChart"></canvas>
            </div>
        </div>
        <div id="div-table">
            <table id="reportTable" class="table">
                <thead>
                    <tr>
                        <th scope="col">STT</th>
                        <th scope="col">Thể Loại</th>
                        <th scope="col">Lượt Mượn</th>
                        <th scope="col">Tỷ Lệ</th>
                    </tr>
                </thead>
                <tbody>
                    <% reportArray.forEach((reportArray, i) => { %>
                        <tr key= <%=reportArray.bookCategory%> >
                            <td scope="col" class="text-wrap">
                                <%= i + 1 %>
                            </td>
                            <td scope="col" class="text-wrap">
                                <%= reportArray.bookCategory.ten_the_loai %>
                            </td>
                            <td scope="col" class="text-wrap">
                                <%= reportArray.count %>
                            </td>
                            <td scope="col" class="text-wrap">
                                <%= (100*(reportArray.count/ sum )).toFixed(1) %>%
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>



